"""The capstone package.

Ava lives in ava.py as an importable module rather than only inside the notebook, because
capstone/test_capstone.py has to be able to reach her. The notebook imports from here and
demonstrates; the specification in README.md is what actually decides whether it works.
"""
